class TaskHandler {
  task = undefined;
  constructor(task) {
    if (task) {
      this.task = task;
    }
    // if (task && Array.isArray(task)) {
    // } else if (task && typeof task === 'object') {
    // }
  }
}
